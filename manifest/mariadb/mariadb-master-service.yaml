apiVersion: v1
kind: Service
metadata:
  name: mariadb-master
  namespace: keycloak
spec:
  selector:
    app: mariadb
    role: master
  ports:
  - port: 3306
    targetPort: 3306
    name: mysql
  type: ClusterIP
